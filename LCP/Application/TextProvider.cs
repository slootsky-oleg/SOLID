namespace LCP.Application
{
    public class TextProvider
    {
        public static string Get(string key)
        {
            //get key from string-h. Ignore language for simplicity
            return "text from string-h";
        }
    }
}